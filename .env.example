# Supabase (project-specific — see Step 7)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Resend (see Step 8)
RESEND_API_KEY=

# Upstash rate limiting (see Step 10)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Preview guard secret — set on preview host only, never on production host, never committed
PREVIEW_SECRET=
